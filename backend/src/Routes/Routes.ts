import RoutesTimes from './RoutesTimes';
import RoutesUser from './RoutesUser';
import RoutesPartidas from './RoutesPartidas';

const routes = [RoutesTimes, RoutesUser, RoutesPartidas];

export default routes;
